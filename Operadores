//Operadores
//No exemplo abaixo, usamos o operador + para somar dois valores:
//Operador + (Adição)
{
int sum1 = 100 + 50;        // 150 (100 + 50)
int sum2 = sum1 + 250;      // 400 (150 + 250)
int sum3 = sum2 + sum2;     // 800 (400 + 400)
}
//---------------------------------------------------------------------------------------------------------------------------------
//Operador - (Subtração)
{
int sum1 = 100 - 50;        // 50 (100 - 50)
int sum2 = sum1 - 250;      // 100 (150 - 250)
int sum3 = sum2 - sum2;     // 0 (400 - 400)
}
//---------------------------------------------------------------------------------------------------------------------------------
//Operador * (Multiplicação)
{
int sum1 = 100 * 50;        // 5 (1 * 5)
int sum2 = sum1 * 250;      // 10 (2 * 5)
int sum3 = sum2 * sum2;     // 16 (4 * 4)
}
//---------------------------------------------------------------------------------------------------------------------------------
//Operador / (Divisão)
{
int sum1 = 100 / 50;        // 5 (1 / 5)
int sum2 = sum1 / 250;      // 3 (2 / 6)
int sum3 = sum2 / sum2;     // 2 (4 / 2)
}
//---------------------------------------------------------------------------------------------------------------------------------